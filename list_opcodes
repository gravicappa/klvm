#!/bin/sh
exclude="
  klvm-lambda
  klvm-runtime
  klvm-trap-error
  klvm-call-error-handler
  klvm-from-kl
  klvm-thaw
"
expr="$(echo $exclude | sed 's/ /\\|/g')"
dir="$(dirname "$0")"
grep -o 'klvm-[a-z0-9<>-]*' <"$dir"/klvm-trans.shen \
| grep -v "$expr" | sort -u
